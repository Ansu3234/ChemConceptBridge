# Server
PORT=5000
JWT_SECRET=change-this-to-a-long-random-string

# Database (MongoDB URI)
# Example (MongoDB Atlas): mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority&appName=<app>
MONGO_URI=

# Google OAuth (Web)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# For local dev, this can be https://developers.google.com/oauthplayground or your app's redirect URL
REDIRECT_URI=
# If using a refresh token flow, store a refresh token with limited scopes
GOOGLE_REFRESH_TOKEN=

# Email (e.g., Gmail App Password or SMTP credentials)
EMAIL_USER=
EMAIL_PASS=

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:3000

# Admin seeding (only used for local/dev seeding)
ADMIN_EMAIL=admin@chemconcept.local
ADMIN_PASSWORD=ChangeMeStrong123!
